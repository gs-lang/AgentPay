modules = ["nodejs-20"]
run = "npm run start"

[deployment]
run = ["sh", "-c", "npm install && npm run build && npm run start"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 3000
externalPort = 80

[env]
PORT = "3000"
